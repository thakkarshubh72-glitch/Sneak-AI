# Place .glb sneaker models in this directory.
# The 3D viewer will attempt to load models from /models/<filename>.glb
# If a model file is missing, the viewer falls back to procedural geometry.
#
# Example sneaker data with 3D model reference:
#   { "model3d": "/models/airforce1.glb" }
#
# Supported format: .glb (GL Binary)
# Recommended: Optimize models under 5MB for mobile performance.
